import {Dirs} from 'react-native-file-access';

// AsyncStorage
export const STORAGE_KEYS = {
  showRegionName: 'showRegionName',
  adMapCount: 'adMapCount',
  adStoryCount: 'adStoryCount',
};

// FileSystem
export const APP_DOC_DIR = `${Dirs.DocumentDir}/memorymap`;
